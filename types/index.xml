<types>
  <type name="Config" desc="Options for the program.">
    <prop boolean name="silent" default="false">
      Do not print the names of read env variables.
    </prop>
    <prop string name="location" default="~">
      The location where to look up the `.env` file.
    </prop>
    <prop string name="name" default=".env">
      The name of the `.env` file.
    </prop>
  </type>
</types>